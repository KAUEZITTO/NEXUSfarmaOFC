'use server';

/**
 * @fileOverview An AI flow for analyzing inventory and order data.
 *
 * - analyzeData - A function that takes a natural language query and returns an analysis.
 */

// This functionality has been temporarily disabled to resolve a build issue.
export async function analyzeData(query: string): Promise<string> {
    console.warn("Analytics flow is currently disabled.");
    return "O assistente de IA está temporariamente indisponível. Tente novamente mais tarde.";
}
