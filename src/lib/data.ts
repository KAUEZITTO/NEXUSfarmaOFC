import type { Product, Order, Patient, Unit, Dispensation } from './types';

// All mock data is now removed as the app is connected to Firestore.
// The types are still exported for use throughout the application.

export const products: Product[] = []; 
export const units: Unit[] = [];
export const patients: Patient[] = [];
export const orders: Order[] = [];
export const dispensations: Dispensation[] = [];
